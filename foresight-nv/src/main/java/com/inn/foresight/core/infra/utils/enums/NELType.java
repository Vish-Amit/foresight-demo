package com.inn.foresight.core.infra.utils.enums;

/**
 * The Enum NELType.
 */
public enum NELType {

    /** The wifi. */
    WIFI,

    /** The aerial. */
    AERIAL,

    /** The attached. */
    ATTACHED,

    /** The community. */
    COMMUNITY,

    /** The smb. */
    SMB,

    /** The venuebase. */
    VENUEBASE,

    /** The venueplus. */
    VENUEPLUS,

    /** The twh. */
    TWH,

    /** The small cell outdoor. */
    SMALL_CELL_OUTDOOR,

    /** The small cell indoor. */
    SMALL_CELL_INDOOR,

    /** The mme. */
    MME,

    /** The saegw. */
    SAEGW,

    /** The css. */
    CSS,

    /** The macro. */
    MACRO,

    /** The macro cell. */
    MACRO_CELL,

    /** The esr. */
    ESR,

    /** The aar. */
    AAR,

    /** The par. */
    PAR,

    /** The ccr. */
    CCR,

    /** The enb. */
    ENB,

    /** The macro enb. */
    MACRO_ENB,

    /** The fibertake. */
    FIBERTAKE,

    /** The microwave. */
    MICROWAVE,

    /** The cs. */
    CS,

    /** The dra. */
    DRA,

    /** The hlr. */
    HLR,

    /** The ngmgw. */
    NGMGW,

    /** The xgw. */
    XGW,

    /** The agr. */
    AGR,

    /** The pe. */
    PE,

    GALLERY, SHOOTER, IBS, PICO,

    /** The p. */
    P,

    /** The cor. */
    COR,

    /** The acc. */
    ACC,

    AGW,

    ASBC,

    CCPS,

    EPDG,

    CSCF,

    IPSM,

    MRF,

    PRS,

    PVGW,

    RCS,

    SPS,

    TAS,

    WEBRTC,

    XDMS,

    IP,

    CE,

    RR,

    IDSC_CELL,

    IDSC_SITE,

    ODSC_CELL,

    ODSC_SITE,

    /** The building. */
    BUILDING,
    /** The boundary. */
    BOUNDARY,
    /** The indoor. */
    INDOOR,

    /** The outdoor. */
    OUTDOOR,

    /** The GC */
    GC,
	
    /** The DC */
    DC,
    
    /** The CDC */
    CDC,
    GALLERY_SITE, 
    PICO_SITE,
    IBS_SITE,
    SHOOTER_SITE ,
    LAB;

}
